# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env.local
.env.test
.env.production
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# plan/scratch files
plan.md
pan.md
*.bak
*.tmp
scratch/

# ai/agents tooling - never commit
.agents/
.cursor/
.gemini/
.claude/
.kiro/
skills-lock.json

# supabase - local dev only
supabase/.temp/
supabase/migrations/
# NOTE: supabase/config.toml can be kept if you want the CLI config tracked.
# Uncomment to also exclude it:
# supabase/config.toml

# dev-only scripts (seeding, schema fixes, storage setup)
/scripts/

# lock files - Vercel generates its own, keeping package.json is enough
# (comment out if you prefer to track lock file for reproducibility)
# package-lock.json
